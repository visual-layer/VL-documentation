# System files
.DS_Store
docs/.DS_Store
api-reference/.DS_Store
docs/Creating-Datasets/importing-annotations/.DS_Store

# Screen recordings
Screen Recording 2025-08-28 at 12.53.35 (1).gif
Screen Recording 2025-08-28 at 12.53.35.mov

# Documentation analysis and working files
docs-inventory.md
docs-inventory.json

# internal-only: only .md, .mdx, .txt, .conf, scripts (.sh, .py) are allowed
# Block all binary/media/document formats
internal-only/**/*.docx
internal-only/**/*.xlsx
internal-only/**/*.pdf
internal-only/**/*.mp4
internal-only/**/*.m4a
internal-only/**/*.mov
internal-only/**/*.aux
internal-only/**/*.log
internal-only/**/*.tex

# Root-level document exports
*.docx
*.pdf
